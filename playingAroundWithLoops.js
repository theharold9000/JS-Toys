var count = 10;
while (count > 0) {
    for (loops = 0; loops < 10; loops++) {
        count = (count - 1);
        var done = false;
        do {
            console.log("It's working!");
        }
        while(done);
    }
}